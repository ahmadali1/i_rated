<nav class='navbar navbar-default'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <button type='button' class='navbar-toggle' data-toggle='collapse' data-target='#myNavbar'>
          <span class='icon-bar'></span>
          <span class='icon-bar'></span>
          <span class='icon-bar'></span>
      </button>
      <a class='navbar-brand' href='#'>I_Rated</a>
    </div>
    <div class='collapse navbar-collapse' id='myNavbar'>
      <ul class='nav navbar-nav'>
        <li class='<%= active_class(root_path) %>'>
          <%= link_to 'Home', root_path %>
        </li>
        <li class='<%= active_class(movies_path) %>'>
          <%= link_to 'Movies', movies_path %>
        </li>
      </ul>

      <div class='col-sm-3 col-md-3'>
        <%= form_tag movies_path, method: :get, class: 'navbar-form', role: 'search' do %>
          <div class='input-group'>
            <%= text_field_tag :search, params[:search], class:'form-control', placeholder: 'Search Movies' %>
            <div class='input-group-btn'>
              <%= submit_tag 'Search', class:'btn btn-default', name: nil %>
            </div>
          </div>
        <% end %>
      </div>

      <ul class='nav navbar-nav navbar-right'>
        <li>
          <div class='col-sm-3 col-md-3'>
            <div class='navbar-form'>
              <%= link_to 'Create New Movie', new_movie_path, class: 'btn btn-primary' %>
            </div>
          </div>
        </li>
        <% if user_signed_in? %>
          <li>
            <%= link_to span_elem('log-in') + ' Logout', destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to span_elem('user') + ' Profile', user_path(current_user) %>
          </li>
        <% else %>
          <li>
            <%= link_to span_elem('log-in') + ' Login', new_user_session_path %>
          </li>
          <li>
            <%= link_to span_elem('user') + ' Signup', new_user_registration_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
