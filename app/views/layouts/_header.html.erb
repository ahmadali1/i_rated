<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">I_Rated
      </a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="<%= active_class(root_path) %>">
          <%= link_to 'Home', root_path %>
        </li>
        <li class="<%= active_class(movies_path) %>">
          <%= link_to 'Movies', movies_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to raw('<span class="glyphicon glyphicon-log-in"></span> Logout'), destroy_user_session_path, method: :delete %>
          </li>
          <li>
            <%= link_to raw('<span class="glyphicon glyphicon-user"></span> Edit Profile'), edit_user_registration_path %>
          </li>
        <% else %>
          <li>
            <%= link_to raw('<span class="glyphicon glyphicon-log-in"></span> Login'), new_user_session_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
